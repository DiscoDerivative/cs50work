#include <stdio.h>

int main(int argc, char* argv[])
{

    // Throw error if user doesn't enter a key or adds to many arguments
    if (argc != 2)
    {
        printf("Usage: ./substitution key");
        return 1;
    }
    
}